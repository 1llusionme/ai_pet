# ai_pet 项目清理与完整性检查报告

## 1. 认定为“相关代码”的目录

- `server/`：后端 API、业务逻辑、测试代码。
- `docs/idea2mvp/年轻人风格原型设计/src`：当前前端主代码。
- `ios/NativeChatScaffold` 与 `ios/HybridAppScaffold`：iOS 端原生/混合壳代码。
- `docs/plans/`：上线、iOS、部署等执行文档。

## 2. 认定为“无效/可再生成文件”的范围

- Python 虚拟环境：`.venv-py313`
- 缓存目录：`.pytest_cache`、`__pycache__`
- 前端依赖与构建产物：`node_modules`、`dist`
- 明显无效文件：根目录 `requirements.txt`（内容为 `404: Not Found`）、空文件 `0.7:`

## 3. 已转移目标目录

- 归档目录：`_archived_invalid_2026-03-19/`
- 转移清单记录：`_archived_invalid_2026-03-19/moved_items.txt`

## 4. 完整性验证结果（转移前已执行）

- 前端验证：
  - 在 `docs/idea2mvp/年轻人风格原型设计` 执行 `npm install && npm run build`
  - 构建通过。
- 后端验证：
  - 使用 Python 3.13 虚拟环境安装 `server/requirements.txt`
  - 执行 `pytest server/tests/test_api_flows.py`
  - 结果：`39 passed`
  - 启动验证：`python -m server.app` 可正常监听 `5001`

## 5. 当前是否需要继续 debug

- 代码层面：暂未发现阻断问题，当前无需继续 debug。
- 运行层面：由于依赖目录已归档，若要再次本地运行，需要先恢复或重装依赖。

## 6. 恢复运行命令（如需）

### 前端

```bash
cd "/Users/liyulin/ai_pet/docs/idea2mvp/年轻人风格原型设计"
npm install
npm run dev -- --host 0.0.0.0 --port 5173
```

### 后端

```bash
cd /Users/liyulin/ai_pet
python3.13 -m venv .venv-py313
./.venv-py313/bin/pip install -r server/requirements.txt
./.venv-py313/bin/python -m server.app
```
